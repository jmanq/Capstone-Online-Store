import React from 'react';

const Cart = ({addToCart}) => {
    return (
        <li className="nav-item">
            <a className="nav-link" href="#">Cart 🛒</a>
        </li>
    );
};

export default Cart;